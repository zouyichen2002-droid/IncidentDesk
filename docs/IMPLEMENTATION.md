# 实施状态

唯一基线为`docs/PRD.md` v1.1（保留 v1.0 门槛）。M0–M5 均已推进到代码、脚本和本机实验；阶段的全部退出条件仍以逐项验收矩阵为准，外部条件未完成不得宣称全部 P0。

| 阶段 | 本地交付与证据 | 剩余条件 |
|---|---|---|
| M0 | 演示服务、真实故障、24变体、角色矩阵、锁定依赖；四源人工调查路径可复查 | 独立远程演示Git仓库配置 |
| M1 | Ontology、映射、约束计划、四源/集群、ContextBundle、证据/trace/录制；基线已跑通 | 无本地阻塞 |
| M2 | Go审批/动作、Runtime SDK、LangGraph、真实Deep Agents录制模型测试、MCP、普通Context API、固定沙箱 | 真实 Mistral 本地已接通；真实 GitHub 写入待完成 |
| M3 | 租约代次、权限撤销、取消、限额、恢复、幂等回调、unknown核对；负向/故障实测 | 真实平台集成边界复验 |
| M4 | Compose/Helm、多副本、HPA、SSE、20RPS/10分钟、100任务、备份恢复、监控与CI配置 | 远端CI首次执行（无remote） |
| M5 | 离线回放、6Hook、badcase审核、企业记忆、48规则运行、退化门禁、模块扰动、覆盖缺口报告 | 人工评分/badcase/试用；真实模型对比与消融 |

主机：Apple Silicon、10逻辑核、32GiB。压测门槛未下调：20RPS持续600秒，元数据API p95<500ms，非预期错误率<1%。第一次失败和第二次通过均保存。真实模型时延、费用、质量不从确定性桩推断。


2026-09-24 v1.1：自然语言入口、Mistral、大数据索引/分页/稀有模式与历史来源匹配的最新实测见 [SEARCH_V2.md](SEARCH_V2.md)。以上旧性能、K8s 与确定性模型证据保持原始范围，不自动外推到新版。
